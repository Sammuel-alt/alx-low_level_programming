#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Prints out all single digit numbers of base 10 starting from 0.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
	printf("%d\n", i);
	}
	return (0);
}
